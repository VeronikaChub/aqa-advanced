const tablenumber = 2;

for (let row = 1; row <= 10; row++) {
    console.log(`${tablenumber} * ${row} = ${tablenumber * row}`);
}

let row = 1;
while (row <= 10) {
    console.log(`${tablenumber} * ${row} = ${tablenumber * row}`);
    row++;
}